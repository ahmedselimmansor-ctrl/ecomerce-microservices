-- ============================================================================
--  العملة الافتراضية: جنيه مصري لا درهم إماراتي.
--
--  ترحيل جديد لا تعديل على V1 — للسبب نفسه الموضّح في ترحيل order-service
--  المقابل: بصمة الترحيل المُطبَّق لا تُمسّ.
--
--  المدفوعات القائمة تبقى بعملتها: مبلغ محصَّل بعملة لا يُعاد تفسيره بأخرى.
-- ============================================================================

ALTER TABLE payments ALTER COLUMN currency SET DEFAULT 'EGP';
